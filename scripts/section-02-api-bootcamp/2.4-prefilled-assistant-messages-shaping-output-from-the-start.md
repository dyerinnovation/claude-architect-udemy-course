# Lecture 2.4: Prefilled Assistant Messages: Shaping Output from the Start

**Section**: Claude API Fundamentals Bootcamp
**Duration**: ~10 minutes
**Status**: Ready to record

---

## SLIDE 1: The Preamble Problem

**Visual**: Cover slide. Title: "Prefilled Assistant Messages". Subtitle: "Shaping Output from the Start". Section badge: Section 2 · Lecture 2.4 · Domain 2.

You asked Claude for clean JSON. You got "Sure! Here's the JSON you asked for…" then the JSON.

In a chat app that's harmless. In a production pipeline, it breaks your parser on the first character. The fix is one of the most useful tools in your API toolkit — and it's not a parameter at all.

Coming up: how this lecture fits into the bootcamp.

---

## SLIDE 2: How This Lecture Fits Into the Course

**Visual**: `<LectureContext>` component. Three tiles: (1) where we came from — controlling randomness with temperature in 2.3, (2) what this lecture is about — prefilled assistant messages, (3) where we're going next — stop sequences as the closing bookend.

Here's where this lecture sits. Lecture 2.3 covered the three randomness dials. Useful for shaping the texture of Claude's output, but they don't control where the response starts.

[click]

This lecture is about a different kind of control — prefilling. You use the structure of the `messages` array itself to force Claude's response to begin with text YOU provide. There's no parameter; the trick lives entirely in how you shape the last message.

[click]

After this, lecture 2.5 covers stop sequences — the matching bookend that controls where Claude stops. Prefill plus stop sequence is the cleanest extraction pattern in the API. Next up, the four things you'll know how to do when this lecture ends.

---

## SLIDE 3: What You'll Learn

**Visual**: `<BulletReveal>` component with 4 bullets revealed on click. Each bullet has a label + a short detail.

Here are the four things you'll walk away knowing.

[click]

First, exactly what a prefill is — and the one-rule mechanic that makes it work, with zero special parameters.

[click]

Second, the four highest-value uses — JSON extraction, killing preamble, code-fence forcing, and persona locking.

[click]

Third, how to read the response body correctly after a prefill — the catch that trips up most first-time users.

[click]

Fourth, why prefills pair perfectly with stop sequences for clean structured extraction. Let's start with the mechanic itself.

---

## SLIDE 4: What Is a Prefill?

**Visual**: A diagram of the messages array as a vertical list of labeled boxes. The boxes are: `user` (blue), `assistant` (green), `user` (blue), and then a final `assistant` box (green, highlighted with a glow) labeled "PREFILL — Claude continues from here." An arrow points from the bottom of the prefill box downward into a response stream.

The `messages` parameter in the API is just an array of turns, where each turn has a role of either "user" or "assistant". To understand prefilling, look at it from two angles side by side.

[click]

On the left, the visualization. A normal conversation alternates user, assistant, user — and then Claude writes the next assistant turn. A prefill flips that final move: you end the array with your own assistant turn, and Claude continues from there.

[click]

On the right, the mechanic. Normally — last entry is `user`, Claude writes a fresh assistant turn. Prefill — last entry is `assistant`, and Claude continues from that exact character. The effect: Claude cannot add anything before your text. You control the very first token of the response. Next, the four situations where prefilling earns its keep.

---

## SLIDE 5: When Prefilling Is the Right Tool

**Visual**: A four-item icon list on a clean white background. Each item has a small icon and a short label: (1) curly braces icon — "Force JSON output", (2) speech bubble with X — "Eliminate filler preamble", (3) code block icon — "Start a code fence", (4) theater mask icon — "Lock in a persona or voice".

Prefilling is useful in four common situations. All of them share the same mechanic — you're not adding a special parameter, you're using the structure of the `messages` array itself.

[click]

First — force JSON to open with an opening brace. The single most common use case. Your downstream parser expects valid JSON from character one, and a prefill guarantees that.

[click]

Second — eliminate preamble. No more "Certainly!" or "Great question!" cluttering the response. The prefill skips straight to the content you want.

[click]

Third — open a code block. Prefill with three backticks and a language tag, and Claude writes Python code directly. No introduction, no explanation first.

[click]

Fourth — lock in a persona. Mid-conversation, you can prefill with a character's signature opener and Claude stays in voice — no immersion break. Next, what this looks like in code, end to end.

---

## SLIDE 6: The Code — JSON Extraction Prefill

**Visual**: Dark-background code editor panel showing the full Python example below, with syntax highlighting. Three lines are highlighted in amber: the prefill message dict, the `content` value of the opening curly brace, and the line accessing the response text. A sidebar note reads: "Claude completes the JSON from here."

Let's walk through a real prefill example end to end. We start the usual way — import anthropic and json, create the client, and define some unstructured input we want to turn into JSON. Nothing prefill-specific yet.

```python
import anthropic
import json

client = anthropic.Anthropic()

user_data = "Name: Alice Chen, Role: Staff Engineer, Team: Platform"

response = client.messages.create(
    model="claude-opus-4-7",
    max_tokens=256,
    messages=[
        {
            "role": "user",
            "content": f"Extract the following into JSON with keys name, role, team:\n\n{user_data}"
        },
        {
            # Prefill: Claude will continue from this exact character
            "role": "assistant",
            "content": "{"
        }
    ]
)

# Re-attach the opening brace Claude was given, then parse
raw = "{" + response.content[0].text
result = json.loads(raw)
print(result)
```

[click]

Now the call itself — and this is where the prefill lives. Look at the messages array. The user turn asks for JSON. But the last entry has role of assistant with content of exactly one character — an opening curly brace. That single brace IS the prefill. Claude continues from that character — it cannot add a preamble before it.

[click]

Here's the detail that trips people up: the prefill text is NOT included in the response body. Claude returns only what it generated AFTER the prefill — so the response text starts with the SECOND character of the JSON, not the opening brace. To reconstruct the full value, you prepend the prefill yourself. That's the `raw` line that concatenates the opening brace back on. Now `json dot loads` works on the first try. Next, two more high-value prefill patterns.

---

## SLIDE 7: Code Generation and Format Prefills

**Visual**: Two side-by-side code panels. Left panel shows a prefill of three backticks plus "python" on a new line with a label "Forces Python code block". Right panel shows a prefill of "The answer is:" with a label "Forces specific phrasing". Both panels have a downward arrow labeled "Claude continues here."

The same technique works for any output format you need to lock in.

For code generation, prefill with the opening fence and language tag.

```python
{
    "role": "assistant",
    "content": "```python\n"
}
```

Claude will now write Python code directly — no introduction, no explanation first.

[click]

For format control, prefill with the exact phrase you want to start the response.

```python
{
    "role": "assistant",
    "content": "The answer is:"
}
```

This eliminates any variation in how Claude opens its response.

It's especially useful when you're parsing outputs downstream with string operations. Next, the pattern that makes prefill truly powerful — pairing it with stop_sequences.

---

## SLIDE 8: Prefills and Stop Sequences — The Power Pair

**Visual**: A flow diagram showing three stages left to right: a "Prefill" box (green, labeled "Controls the START"), a middle box showing Claude's generation in progress, and a "Stop Sequence" box (red, labeled "Controls the END"). An arrow runs through all three labeled "Clean, bounded output."

Prefills and stop sequences solve opposite halves of the same problem.

A prefill controls where Claude starts. A stop sequence controls where Claude stops.

Together, they let you extract exactly what you want from a response.

[click]

Here's the pattern: prefill with an opening XML tag, stop at the closing tag.

```python
response = client.messages.create(
    model="claude-opus-4-7",
    max_tokens=512,
    stop_sequences=["</answer>"],
    messages=[
        {"role": "user", "content": "What is 12 * 8? Respond inside <answer> tags."},
        {"role": "assistant", "content": "<answer>"}  # prefill
    ]
)
# response.content[0].text contains only what's between the tags
```

The response will be the content between the tags — nothing before, nothing after. That's a clean extraction with zero post-processing regex. Next, the exam trap most candidates fall into.

---

## SLIDE 9: Exam Tip

**Visual**: Amber callout box. Title: "Exam Tip". Icon: target/bullseye. Text: "There is no `prefill` parameter — it's just the messages array."

Exam Tip time. The headline is blunt — there is no `prefill` parameter. Two callouts make that concrete.

[click]

First, the distractor pattern. Candidates look for a dedicated `prefill` parameter in the API, or believe you need a special flag like `prefill=True` to enable this behavior. There is no such parameter. A distractor that invents one is plausible in a different API's context — which is exactly what makes it an almost-right trap on the exam.

[click]

Second, the entire mechanic — and it really is the entire mechanic. If the last message in the array has role of user, Claude starts a fresh response. If it has role of assistant, Claude continues from its content. That's it. Know that one rule cold and prefill questions on the exam are free points. Last up, the takeaways to lock in.

---

## SLIDE 10: Key Takeaways

**Visual**: Clean bullet list on light background. Title: "What to Remember".

Four things to hold onto from this lecture.

[click]

First — assistant-role last. A prefill is an assistant-role message placed LAST in the `messages` array. Claude continues from its content, adding nothing before it. That's the whole technique in one sentence.

[click]

Second — not in the response body. The prefill text is NOT included in the response body. Claude returns only what it generated after the prefill, so you prepend the prefill yourself when reconstructing the full output.

[click]

Third — common uses. Force JSON to open with an opening brace. Eliminate preamble. Open code fences with a language tag. Lock in a persona mid-conversation.

[click]

Fourth — pair with stop_sequences. Prefill opens, stop sequence closes. The cleanest way to extract structured content with zero post-processing regex. In the next lecture, we'll go deep on stop sequences themselves — how Claude matches them, the gotchas, and why they're the production-grade way to bound an output.
